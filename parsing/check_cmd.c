#include <minishell.h>

//check si la commande exist bien dans le path, donc je dois aller dans le env, chercher le path et aller check dans chaque categorie si la commande exist
// si la commande existe pas check si bultin, ou autre.
